# Cursor adapter

Read and obey `AGENTS.md`. Treat `university/` as canonical.

Use `.cursor/agents/university-*.md` as Profession subagents. Cursor subagents have isolated context, so every delegation must include the target Worker and activation context required by `university/protocols/worker-activation.md`.

Use `university-*` Skills for reusable operations; do not substitute a subagent for a Skill. Project rules under `.cursor/rules/` enforce the canonical University boundary.

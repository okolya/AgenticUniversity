# Rector baseline skills

- `rector-startup`
- `appoint-worker`
- `inspect-student-state`
- `research-materials` only when institution-level research is required

`register-worker` is normally invoked internally by `appoint-worker`. Rector coordinates institution-level workflows and appointments but must not perform Dean Faculty decisions unless separately appointed to that Profession/scope.
